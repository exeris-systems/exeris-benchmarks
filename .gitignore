target/
!targets/exeris-benchmark-app/src/main/java/eu/exeris/kernel/benchmark/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
# https://maven.apache.org/wrapper/#usage-without-binary-jar
.mvn/wrapper/maven-wrapper.jar

# Eclipse m2e generated files
# Eclipse Core
.project
# JDT-specific (Eclipse Java Development Tools)
.classpath

# Exeris Benchmarks — additional ignores
# IDE
.idea/
*.iml
.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# Raw large result files — keep normalized/ and history/ only
# Uncomment if results/raw/ grows too large:
# results/raw/*.txt
# results/raw/k6-*.json

# Raw JFR recordings are NEVER committed. Default publication mode is `public`,
# which default-denies raw .jfr (extension + FLR\0 signature); Exeris recordings
# also carry custom telemetry. Publish derived artifacts only (flamegraph SVGs,
# extracted metrics JSON). For restricted sharing use publish-report.sh
# --publication-mode internal-only.
*.jfr
*.JFR

# h2load per-request --log-file dumps are huge (100+ MB each); the derived
# h2load-latency.json carries the percentiles. (Covered by *.log below too.)

# Environment files with real values — template is committed, actuals are not
runtime/drivers/env/*.env
!runtime/drivers/env/template.env

# Exeris enterprise confidentiality — enterprise provider classes in git-ignored env files
enterprise/env/enterprise-sysprops.env
env/.local/

# Temp
*.tmp
*.log

# Upstream installer scripts must not be committed
get-docker.sh

# Comparative benchmark quarantine and audit logs
quarantine/
comparative-audit.log
**/target-a-pidfile
**/target-b-pidfile
**/sync-log.txt

