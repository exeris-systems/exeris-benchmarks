{
  "scenario_id": "destructive-radamsa-h1",
  "version": "1",
  "benchmark_family": "runtime",
  "target_classification": "runtime-destructive-malformed-input",
  "description": "Mutated-input attack against an externally-launched HTTP/1.1 target. A seed request (GET /plaintext) is mutated by radamsa (https://gitlab.com/akihe/radamsa) into a stream of malformed HTTP/1.1 requests, each fired over a fresh TCP connection. Whatever the mutated bytes are, the target MUST NOT crash, OOM, or hang. PRIMARY CHECK: after attack window + cooldown, target serves clean GET /health with 200 OK. SECONDARY CHECK: process RSS delta within tolerance (no leaked Arenas).",
  "mode": "destructive-malformed-input",
  "tier": "community",
  "driver": ["radamsa"],
  "transport": ["h1"],
  "loopback": true,
  "loopback_caveat": "Loopback transport. transport_mode=loopback-h1 mandatory.",
  "attack_profile": {
    "requests_per_second": 500,
    "attack_duration_seconds": 120,
    "cooldown_seconds": 30,
    "radamsa_seed": "REQUIRED — set via --radamsa-seed; without this the campaign is not reproducible",
    "concurrent_connections": 256,
    "socket_timeout_seconds": 2.0,
    "rate_note": "requests_per_second is a REQUESTED rate. Until 2026-08-26 the driver was single-threaded and every read timeout blocked it for the full socket deadline, so the first campaign requested 500 and achieved 3.4 (60 timeouts x 2.0 s consumed the whole 120 s window; radamsa generation was 1.1 % of it). The driver now fires from a worker pool and reports achieved_rps and backlog_skips -- a run that cannot reach this rate says so instead of letting this number stand in for what happened.",
    "concurrency_note": "Concurrency is part of the stimulus, not a harness detail: N connections at once is a different attack from N in sequence. It travels with the result and belongs in any comparison. Sizing: rps x incomplete-mutant fraction x socket timeout gives the worker-seconds per wall second the timeouts alone consume."
  },
  "liveness_probe": {
    "mandatory": true,
    "method": "GET",
    "path": "/health",
    "expected_status": 200,
    "max_response_ms": 1000
  },
  "tolerance": {
    "rss_growth_pct_max": 5,
    "max_unexpected_crashes": 0,
    "max_hang_count": 0,
    "hang_note": "hang_count counts read timeouts on mutants that COMPLETED a request, where the target owed an answer. A timeout on a truncated mutant is incomplete-wait: the target is correctly waiting for the rest and the attacker gave up first. Before this split every timeout was a hang, which made max_hang_count unusable -- the first campaign logged 60 while the target answered /health in 8 ms. Whether the target EVER times out an incomplete request is destructive-slowloris-h1's question, not this one."
  },
  "claim_scope": "exploratory",
  "comparison_axis_note": "Cross-stack radamsa results require IDENTICAL --radamsa-seed for fair comparison. Different seeds produce different inputs; comparing different seeds is meaningless. Label the seed in any cross-stack report row.",
  "publication_mode_required": "internal-only",
  "publication_mode_reason": "Radamsa-mutated payloads written to disk on crash (crash-*.radamsa) contain attacker-supplied bytes; basename blocklist in publish-report.sh blocks them outside internal-only mode."
}
