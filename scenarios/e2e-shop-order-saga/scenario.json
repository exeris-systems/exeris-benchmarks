{
  "scenario_id": "e2e-shop-order-saga",
  "version": "1",
  "schema_version": "2",
  "contract_revision": "2.0",
  "contract_authority": "scenarios/e2e-shop-order-saga/CONTRACT-v2.md",
  "contract_revision_note": "CONTRACT-v2.md v2.0 (DRAFT) is the authoritative specification for this scenario. The fixed_contracts in this file remain the v1-active machine contracts and are NOT asserted to be v2-machine-compliant: the deterministic per-orderId decline, durability-tier declaration, and outcome-split reporting live in scripts/run-e2e-shop-order-saga-baseline.sh and are stamped into run artifacts, not into these static definitions. Per-section v2 enforcement status (implemented-now / partial / deferred) is in scenarios/e2e-shop-order-saga/CONTRACT-v2-IMPLEMENTATION.md. Do not read contract_revision=2.0 as a claim that any fixed_contract below has passed the v2 correctness gates.",
  "benchmark_family": "runtime",
  "target_classification": "runtime-e2e-baseline",
  "description": "End-to-end shop order saga: user registration → graph-based product recommendations → cart operations → order placement → multi-step saga orchestration with compensation",
  "endpoints": [
    "POST /api/v1/auth/register",
    "GET /api/v1/products/recommended",
    "POST /api/v1/cart/add",
    "GET /api/v1/cart",
    "POST /api/v1/orders"
  ],
  "mode": "stateful-e2e-saga",
  "tier": "community",
  "driver": [
    "k6"
  ],
  "transport": [
    "h2c"
  ],
  "loopback": true,
  "loopback_caveat": "Results are not equivalent to network-path benchmarks. transport_mode=loopback-h2c is mandatory in all result artifacts.",
  "claim_scope": "exploratory",
  "concurrency": {
    "virtual_users": 100,
    "ramp_up_time": "1m",
    "warmup_time": "1m",
    "measurement_time": "3m",
    "think_time_ms_min": 800,
    "think_time_ms_max": 2500,
    "note": "100 VUs with 800-2500ms think-time between saga steps"
  },
  "db": {
    "type": "postgresql",
    "image": "postgres:16.2",
    "schema_migration_version": "V3_E2E_SAGA_KERNEL_OUTBOX",
    "connection_pool_size": 20
  },
  "graph_backend": {
    "type": "neo4j",
    "image": "neo4j:5.16-community",
    "driver": "bolt",
    "connection_pool_size": 10
  },
  "graph_tracks": {
    "neo4j": {
      "track_id": "track_a_neo4j",
      "backend_family": "neo4j",
      "backend_mode": "neo4j-bolt",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-inline-r50-v2",
      "required_contracts": [
        "exeris_community_h1_v2",
        "quarkus_axon_neo4j_h1_v2",
        "spring_boot_axon_neo4j_h1_v2",
        "spring_runtime_on_exeris_flow_neo4j_h1_v1"
      ],
      "claim_scope": "comparison_eligible",
      "comparison_class": "platform-natural-stack",
      "equivalence_note": "Saga orchestration via each platform recommended approach: exeris=flow-native, quarkus=axon-framework (synchronous in request path), spring=axon-framework (async tracking processor, tuned to 16 segments). Not algorithmically equivalent. As of 2026-07-30 all three resolve the terminal outcome INLINE, so end-to-end saga duration is measured identically and order_create_latency_ms is comparison-eligible. spring_runtime_on_exeris_flow_neo4j_h1_v1 remains POLLED and exploratory-only - drivable on this track but not part of the comparison set.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "comparison_eligible",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      }
    },
    "pgq_pure": {
      "track_id": "track_b1_pgq_pure",
      "backend_family": "postgres-graph",
      "backend_engine": "pgq",
      "backend_mode": "pgq-pure",
      "graph_query_model": "pgq-sql",
      "workload_profile_key": "e2e-shop-order-saga-community-h2c-loopback-runtime-k6-pgq-pure",
      "required_contracts": [
        "exeris_community_h2c_pgq_v2",
        "quarkus_axon_pgq_v2",
        "spring_boot_axon_pgq_v2"
      ],
      "claim_scope": "coverage_limited_until_all_targets_eligible",
      "backend_equivalence_class": "postgres_graph_pgq_pure",
      "comparison_class": "platform-natural-stack",
      "equivalence_note": "Saga orchestration via platform-recommended approach: exeris=flow-native, quarkus=axon-framework, spring=axon-framework. Not algorithmically equivalent. Comparison reflects real-world platform adoption cost, not isolated runtime performance."
    },
    "age_compat": {
      "track_id": "track_b2_age_compat",
      "backend_family": "postgres-graph",
      "backend_engine": "apache-age",
      "backend_mode": "age-compat",
      "graph_query_model": "opencypher-age",
      "workload_profile_key": "e2e-shop-order-saga-community-h2c-loopback-runtime-k6-age-compat",
      "required_contracts": [
        "exeris_community_h2c_age_compat_v1",
        "quarkus_axon_age_compat_v1",
        "spring_boot_axon_age_compat_v1"
      ],
      "claim_scope": "compatibility_only_until_full_parity",
      "backend_equivalence_class": "postgres_graph_age_compat",
      "comparison_class": "platform-natural-stack",
      "equivalence_note": "Saga orchestration via platform-recommended approach: exeris=flow-native, quarkus=axon-framework, spring=axon-framework. Not algorithmically equivalent. Comparison reflects real-world platform adoption cost, not isolated runtime performance."
    },
    "pgq": {
      "track_id": "track_b_pgq",
      "backend_family": "postgres-graph",
      "backend_mode": "pgq",
      "workload_profile_key": "e2e-shop-order-saga-community-h2c-loopback-runtime-k6-pgq",
      "required_contracts": [
        "exeris_community_h2c_pgq_v2",
        "quarkus_axon_pgq_v2",
        "spring_boot_axon_pgq_v2"
      ],
      "claim_scope": "coverage_limited_until_all_targets_eligible",
      "deprecated": true,
      "alias_of": "pgq_pure",
      "notes": "Use pgq_pure or age_compat; do not aggregate alias with new tracks"
    },
    "none": {
      "track_id": "track_c_no_graph",
      "backend_family": "none",
      "backend_mode": "none",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "_note": "CONTRACT-v2 §2.1 shape A. The graph was removed from this scenario (§2) because it confounded the only comparison the scenario exists to make, so this track has no graph backend at all — Neo4j is not in the §1 deployment unit and is not started. track_id is an isolation boundary: results here MUST NOT be aggregated with track_a_neo4j.",
      "required_contracts": [
        "exeris_community_h1_park100_v3",
        "quarkus_lra_jdbc_h1_park100_v3",
        "spring_axon_jdbc_h1_park100_v3",
        "spring_axon_embedded_jdbc_h1_park100_v3",
        "restate_saga_h1_park100_v3"
      ]
    }
  },
  "target_backend_support": {
    "exeris-community": [
      "neo4j",
      "postgres-graph"
    ],
    "quarkus-hibernate": [
      "neo4j",
      "postgres-graph"
    ],
    "spring-hibernate": [
      "neo4j",
      "postgres-graph"
    ],
    "spring-on-exeris": [
      "neo4j",
      "postgres-graph"
    ],
    "spring-axon-embedded": [
      "neo4j",
      "postgres-graph"
    ],
    "quarkus-lra-jdbc": [
      "neo4j",
      "postgres-graph"
    ],
    "spring-axon-jdbc": [
      "neo4j",
      "postgres-graph"
    ],
    "spring-axon-embedded-jdbc": [
      "neo4j",
      "postgres-graph"
    ]
  },
  "target_backend_support_status": {
    "exeris-community": {
      "neo4j": "eligible",
      "postgres-graph": "eligible",
      "pgq-pure": "eligible",
      "age-compat": "planned"
    },
    "quarkus-hibernate": {
      "neo4j": "eligible",
      "postgres-graph": "planned",
      "pgq-pure": "planned",
      "age-compat": "eligible"
    },
    "spring-hibernate": {
      "neo4j": "eligible",
      "postgres-graph": "planned",
      "pgq-pure": "planned",
      "age-compat": "eligible"
    },
    "spring-on-exeris": {
      "neo4j": "eligible",
      "postgres-graph": "planned",
      "pgq-pure": "planned",
      "age-compat": "planned"
    }
  },
  "claim_guardrails": {
    "no_cross_backend_claims": true,
    "no_mixed_baseline_aggregate_claims": true,
    "require_track_label_in_every_claim": true,
    "forbid_cross_track_pairing": true,
    "forbid_mixed_backend_equivalence_class": true
  },
  "seed": {
    "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
    "manifest_version": "1",
    "idempotency": "INSERT ... ON CONFLICT (id) DO UPDATE",
    "pre_run_verification": "row_count_and_hash",
    "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh",
    "idempotency_guarantee": "Re-running seed.sql produces identical state across users, products, inventory, product_relationships, and user_purchase_history tables. Results without passing pre-run verification are invalid."
  },
  "workload_invariants": {
    "payment_fail_rate": 0.03,
    "payment_fail_rate_env": "EXERIS_SAGA_PAYMENT_FAIL_RATE",
    "payment_fail_rate_note": "All targets participating in cross-runtime comparison MUST be configured with identical payment_fail_rate. Runs where any target deviates from this value are invalid for comparative purposes. Exeris: configured via EXERIS_SAGA_PAYMENT_FAIL_RATE env var. Quarkus/Spring Axon targets: must configure equivalent fail injection via their own mechanism at the same rate.",
    "fairness_enforcement": "payment_fail_rate_mismatch_is_disqualifying",
    "k6_poll_budget": {
      "max_poll_attempts": 25,
      "poll_interval_seconds": 1,
      "rationale": "25-attempt budget allows compensation (COMPENSATING state) time to reach terminal COMPENSATED state before marking saga as unresolved"
    }
  },
  "fixed_contracts": {
    "exeris_community_h2c_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "exeris-community",
      "comparison_type": "cross-runtime-e2e-saga",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "saga_orchestration": "flow-native",
      "graph_backend": "neo4j-bolt",
      "event_backend": "events-l3-outbox",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "comparison_class": "platform-natural-stack",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "order_create_latency_ms": "coverage_limited_saga_engine_not_equivalent",
        "order_poll_latency_ms": "coverage_limited_saga_engine_not_equivalent"
      },
      "maturity": "superseded",
      "superseded_by": "exeris_community_h1_v2"
    },
    "quarkus_axon_neo4j_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "quarkus-hibernate",
      "framework": "quarkus",
      "saga_framework": "axon-framework",
      "graph_backend": "neo4j-bolt",
      "event_backend": "axon-event-sourcing-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "claim_scope": "coverage_limited_saga_engine_not_equivalent",
      "claim_scope_note": "Axon saga targets record end-to-end HTTP latency under the Axon Framework event-sourcing execution model. Request-path DB writes differ structurally from Exeris community (Axon: in-process command dispatch + in-memory SimpleEventBus event publish — no synchronous DB writes in HTTP request path; Exeris: INSERT orders + INSERT order_items + UPDATE saga_id — 3 synchronous DB writes in HTTP request path). Inventory reservation, multi-step saga state transitions, and compensation are not executed in the request path under Axon. Status poll served from in-memory projection (ConcurrentHashMap), not from a DB read. Cross-target latency comparison with exeris-community is NOT supported at this claim scope.",
      "workload_note": "product_level_including_framework: Axon Framework event-sourcing saga model vs Exeris CoreFlowEngine native saga. order_id is a logical AtomicLong saga correlation identifier, not a DB BIGSERIAL row. status_poll_comparison_excluded: true.",
      "comparison_class": "platform-natural-stack",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "order_create_latency_ms": "coverage_limited_saga_engine_not_equivalent",
        "order_poll_latency_ms": "coverage_limited_saga_engine_not_equivalent"
      },
      "maturity": "superseded",
      "superseded_by": "quarkus_axon_neo4j_h1_v2"
    },
    "spring_boot_axon_neo4j_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "spring-hibernate",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "graph_backend": "neo4j-bolt",
      "event_backend": "axon-event-sourcing-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "claim_scope": "coverage_limited_saga_engine_not_equivalent",
      "claim_scope_note": "Axon saga targets record end-to-end HTTP latency under the Axon Framework event-sourcing execution model. Request-path DB writes differ structurally from Exeris community (Axon: in-process command dispatch + in-memory SimpleEventBus event publish — no synchronous DB writes in HTTP request path; Exeris: INSERT orders + INSERT order_items + UPDATE saga_id — 3 synchronous DB writes in HTTP request path). Inventory reservation, multi-step saga state transitions, and compensation are not executed in the request path under Axon. Status poll served from in-memory projection (ConcurrentHashMap), not from a DB read. Cross-target latency comparison with exeris-community is NOT supported at this claim scope.",
      "workload_note": "product_level_including_framework: Axon Framework event-sourcing saga model vs Exeris CoreFlowEngine native saga. order_id is a logical AtomicLong saga correlation identifier, not a DB BIGSERIAL row. status_poll_comparison_excluded: true.",
      "comparison_class": "platform-natural-stack",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "order_create_latency_ms": "coverage_limited_saga_engine_not_equivalent",
        "order_poll_latency_ms": "coverage_limited_saga_engine_not_equivalent"
      },
      "maturity": "superseded",
      "superseded_by": "spring_boot_axon_neo4j_h1_v2"
    },
    "spring_runtime_on_exeris_flow_neo4j_h1_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h1",
      "target_app": "spring-on-exeris",
      "framework": "spring-boot",
      "framework_version": "3.5.14",
      "host_runtime": "exeris-spring-runtime",
      "host_runtime_mode": "compatibility",
      "saga_framework": "exeris-spring-runtime-flow",
      "graph_backend": "neo4j-bolt",
      "event_backend": "exeris-flow-jdbc-snapshot",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "claim_scope": "exploratory",
      "claim_scope_note": "Compatibility-mode hosting: Spring MVC @RestController dispatch is overlaid on the Exeris ingress via ExerisCompatAutoConfiguration (ADR-011). Saga orchestration is exeris-spring-runtime-flow (4 forward steps: reserve-inventory, charge-payment, confirm-order, complete-order; 2 compensations: refund-payment, restore-inventory) — algorithmically the same Flow engine that exeris-community uses, NOT Axon Framework. Synchronous-insert variant: orders + order_items rows are persisted on the request thread before 202 ACCEPTED returns (idempotent under userId + ':' + cartId), so order_create_latency_ms includes JPA-path INSERT cost (unlike the Axon contract, whose request path was DB-write-free). Status poll reads from the JdbcFlowSnapshotStore-backed projection. JPA telemetry bypass disclosure (ADR-017 §6.4): every JPA Connection acquisition flows through the ExerisDataSource compat seam emitting JpaConnectionAcquired + JpaConnectionBound JFR events; cross-target JPA latency / allocation deltas vs kernel-native (exeris-community) or Hikari-pooled (spring-hibernate, quarkus-hibernate) targets must split 'compatibility seam overhead' from 'engine cost'. Protocol-mode mismatch with sibling contracts (this contract H1 plaintext; exeris_community_h2c_v1 and spring_boot_axon_neo4j_v1 are H2C) — direct latency comparison across protocol modes is NOT eligible at strict gate. event_backend = exeris-flow-jdbc-snapshot reflects that there is no external event store: Flow lifecycle state is durably checkpointed by JdbcFlowSnapshotStore (ADR-022), and the workload's outbox writes (in confirm step) target the existing exeris_outbox table directly via JDBC. Spring Boot 3.5.14 line pinned to host-runtime artifact (exeris-spring-runtime:0.5.0-SNAPSHOT) rather than blueprint's SB4 — bench-implementer divergence justified by host-runtime artifact reality (see target README §'Spring Boot matrix alignment').",
      "workload_note": "saga_engine_identical_to_exeris_community: exeris-spring-runtime-flow uses the same Flow engine class as exeris-community. Hosting axes differ: web ingress = Spring MVC compat overlay on Exeris ingress (this contract) vs Exeris-native ExerisRequestHandler (exeris-community); JPA = ExerisDataSource compat seam (this contract) vs kernel-native QueryExecutor (exeris-community). order_id is a DB BIGSERIAL row id (synchronously persisted), matching exeris-community semantics and differing from the Axon contracts' AtomicLong saga correlation id. status_poll reads from JdbcFlowSnapshotStore projection.",
      "comparison_class": "algorithmic-isomorph-different-hosting",
      "equivalence_note": "Saga engine identical to exeris_community_h2c_v1 (both run exeris-spring-runtime-flow / Exeris FlowEngine). Differs on hosting axes only: web ingress (Spring MVC compat vs Exeris-native) and persistence path (ExerisDataSource compat seam vs kernel-native QueryExecutor). Against spring_boot_axon_neo4j_v1: same web framework family (Spring MVC), different saga engine (Flow vs Axon) and different host runtime (exeris-spring-runtime compat vs Spring-Boot-on-Tomcat). Both cross-contract comparisons are exploratory; protocol-mode mismatch (H1 here vs H2C on both peers) is the dominant disqualifier for strict-gate latency claims.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "exploratory",
        "recommend_latency_ms": "exploratory",
        "cart_add_latency_ms": "exploratory",
        "cart_get_latency_ms": "exploratory",
        "order_create_latency_ms": "exploratory",
        "order_poll_latency_ms": "exploratory"
      }
    },
    "exeris_community_h2c_pgq_v2": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "exeris-community",
      "comparison_type": "cross-runtime-e2e-saga",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "saga_orchestration": "flow-native",
      "graph_backend": "pgq",
      "event_backend": "events-l3-outbox",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "guard",
      "claim_scope": "coverage_limited",
      "comparison_class": "platform-natural-stack",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      }
    },
    "quarkus_axon_pgq_v2": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "quarkus-hibernate",
      "framework": "quarkus",
      "saga_framework": "axon-framework",
      "graph_backend": "pgq",
      "event_backend": "axon-event-sourcing-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "guard",
      "claim_scope": "coverage_limited",
      "claim_scope_note": "Axon saga targets record end-to-end HTTP latency under the Axon Framework event-sourcing execution model. Request-path DB writes differ structurally from Exeris community (Axon: in-process command dispatch + in-memory SimpleEventBus event publish — no synchronous DB writes in HTTP request path; Exeris: INSERT orders + INSERT order_items + UPDATE saga_id — 3 synchronous DB writes in HTTP request path). Inventory reservation, multi-step saga state transitions, and compensation are not executed in the request path under Axon. Status poll served from in-memory projection (ConcurrentHashMap), not from a DB read. Cross-target latency comparison with exeris-community is NOT supported at this claim scope.",
      "workload_note": "product_level_including_framework: Axon Framework event-sourcing saga model vs Exeris CoreFlowEngine native saga. order_id is a logical AtomicLong saga correlation identifier, not a DB BIGSERIAL row. status_poll_comparison_excluded: true.",
      "comparison_class": "platform-natural-stack",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      }
    },
    "spring_boot_axon_pgq_v2": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "spring-hibernate",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "graph_backend": "pgq",
      "event_backend": "axon-event-sourcing-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "guard",
      "claim_scope": "coverage_limited",
      "claim_scope_note": "Axon saga targets record end-to-end HTTP latency under the Axon Framework event-sourcing execution model. Request-path DB writes differ structurally from Exeris community (Axon: in-process command dispatch + in-memory SimpleEventBus event publish — no synchronous DB writes in HTTP request path; Exeris: INSERT orders + INSERT order_items + UPDATE saga_id — 3 synchronous DB writes in HTTP request path). Inventory reservation, multi-step saga state transitions, and compensation are not executed in the request path under Axon. Status poll served from in-memory projection (ConcurrentHashMap), not from a DB read. Cross-target latency comparison with exeris-community is NOT supported at this claim scope.",
      "workload_note": "product_level_including_framework: Axon Framework event-sourcing saga model vs Exeris CoreFlowEngine native saga. order_id is a logical AtomicLong saga correlation identifier, not a DB BIGSERIAL row. status_poll_comparison_excluded: true.",
      "comparison_class": "platform-natural-stack",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      }
    },
    "exeris_community_h2c_age_compat_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "exeris-community",
      "comparison_type": "cross-runtime-e2e-saga",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "saga_orchestration": "flow-native",
      "graph_backend": "apache-age",
      "graph_query_model": "opencypher-age",
      "backend_equivalence_class": "postgres_graph_age_compat",
      "event_backend": "events-l3-outbox",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "planned",
      "claim_scope": "compatibility_only",
      "comparison_class": "platform-natural-stack"
    },
    "quarkus_axon_age_compat_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "quarkus-hibernate",
      "framework": "quarkus",
      "saga_framework": "axon-framework",
      "graph_backend": "apache-age",
      "graph_query_model": "opencypher-age",
      "backend_equivalence_class": "postgres_graph_age_compat",
      "event_backend": "axon-event-sourcing-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "planned",
      "claim_scope": "compatibility_only",
      "claim_scope_note": "Axon saga targets record end-to-end HTTP latency under the Axon Framework event-sourcing execution model. Request-path DB writes differ structurally from Exeris community (Axon: in-process command dispatch + in-memory SimpleEventBus event publish — no synchronous DB writes in HTTP request path; Exeris: INSERT orders + INSERT order_items + UPDATE saga_id — 3 synchronous DB writes in HTTP request path). Inventory reservation, multi-step saga state transitions, and compensation are not executed in the request path under Axon. Status poll served from in-memory projection (ConcurrentHashMap), not from a DB read. Cross-target latency comparison with exeris-community is NOT supported at this claim scope.",
      "workload_note": "product_level_including_framework: Axon Framework event-sourcing saga model vs Exeris CoreFlowEngine native saga. order_id is a logical AtomicLong saga correlation identifier, not a DB BIGSERIAL row. status_poll_comparison_excluded: true.",
      "comparison_class": "platform-natural-stack"
    },
    "spring_boot_axon_age_compat_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h2c",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "transport_mode": "loopback-h2c",
      "target_app": "spring-hibernate",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "graph_backend": "apache-age",
      "graph_query_model": "opencypher-age",
      "backend_equivalence_class": "postgres_graph_age_compat",
      "event_backend": "axon-event-sourcing-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "planned",
      "claim_scope": "compatibility_only",
      "claim_scope_note": "Axon saga targets record end-to-end HTTP latency under the Axon Framework event-sourcing execution model. Request-path DB writes differ structurally from Exeris community (Axon: in-process command dispatch + in-memory SimpleEventBus event publish — no synchronous DB writes in HTTP request path; Exeris: INSERT orders + INSERT order_items + UPDATE saga_id — 3 synchronous DB writes in HTTP request path). Inventory reservation, multi-step saga state transitions, and compensation are not executed in the request path under Axon. Status poll served from in-memory projection (ConcurrentHashMap), not from a DB read. Cross-target latency comparison with exeris-community is NOT supported at this claim scope.",
      "workload_note": "product_level_including_framework: Axon Framework event-sourcing saga model vs Exeris CoreFlowEngine native saga. order_id is a logical AtomicLong saga correlation identifier, not a DB BIGSERIAL row. status_poll_comparison_excluded: true.",
      "comparison_class": "platform-natural-stack"
    },
    "exeris_community_h1_v2": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "neo4j-bolt",
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-inline-r50-v2",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "descriptive_per_stack_only",
      "supersedes_note": "Supersedes the h2c-named v1 contract for this target. Runs under the two MUST NEVER be aggregated: protocol label, terminal-outcome resolution model and arrival rate all changed.",
      "target_app": "exeris-community",
      "framework": "exeris-community",
      "saga_orchestration": "flow-native",
      "event_backend": "events-l3-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "Runs the saga in-process on the request thread and returns the terminal outcome inline. Persists flow state to the Postgres v5 tables, so a share of this stack Postgres cost is saga state - see deployment-footprint.json, which reports it per component. The request thread blocks for the saga duration because FlowScheduler exposes no synchronous execute and no completion handle (section 9(a)).",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "exeris_community_h2c_v1",
      "workload_shape": "A0-straight-through-superseded",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape A0: nothing parks. NOT comparison-eligible. The stacks do not execute the same work — quarkus-hibernate runs the whole saga synchronously on the request thread (transaction script), spring-hibernate runs it async via Axon Server, exeris-community via the flow scheduler. All return the terminal outcome inline, but a shared observable contract is not equivalent execution. order_create_latency_ms was briefly lifted to comparison_eligible on 2026-07-30 on the grounds that the RESOLUTION MODEL was uniform; that was wrong and is reverted — resolution uniformity does not imply execution equivalence. Superseded by shape A (minimal park), where every stack must implement dispatch/park/wake and therefore does the same shape of work.",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "_superseded_by": "exeris_community_h1_park100_v3"
    },
    "quarkus_axon_neo4j_h1_v2": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "neo4j-bolt",
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-inline-r50-v2",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "descriptive_per_stack_only",
      "supersedes_note": "Supersedes the h2c-named v1 contract for this target. Runs under the two MUST NEVER be aggregated: protocol label, terminal-outcome resolution model and arrival rate all changed.",
      "target_app": "quarkus-hibernate",
      "framework": "quarkus",
      "saga_framework": "axon-framework",
      "event_backend": "axon-event-sourcing-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "CORRECTED 2026-07-30. The previous note (shared verbatim with the spring contract) claimed Axon performs no synchronous DB writes in the HTTP request path. That is FALSE for this stack: AxonOrderSagaCommandHandler.handle runs insert-order, reserve-inventory, charge-payment and confirm-order (or the LIFO compensation pair) synchronously on the request thread and returns the terminal outcome in the POST body. Every domain write happens in the request path here.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "quarkus_axon_neo4j_v1",
      "workload_shape": "A0-straight-through-superseded",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape A0: nothing parks. NOT comparison-eligible. The stacks do not execute the same work — quarkus-hibernate runs the whole saga synchronously on the request thread (transaction script), spring-hibernate runs it async via Axon Server, exeris-community via the flow scheduler. All return the terminal outcome inline, but a shared observable contract is not equivalent execution. order_create_latency_ms was briefly lifted to comparison_eligible on 2026-07-30 on the grounds that the RESOLUTION MODEL was uniform; that was wrong and is reverted — resolution uniformity does not imply execution equivalence. Superseded by shape A (minimal park), where every stack must implement dispatch/park/wake and therefore does the same shape of work.",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "_superseded_by": "quarkus_lra_h1_park100_v3"
    },
    "spring_boot_axon_neo4j_h1_v2": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "neo4j-bolt",
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-inline-r50-v2",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "descriptive_per_stack_only",
      "supersedes_note": "Supersedes the h2c-named v1 contract for this target. Runs under the two MUST NEVER be aggregated: protocol label, terminal-outcome resolution model and arrival rate all changed.",
      "target_app": "spring-hibernate",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "event_backend": "axon-event-sourcing-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "CORRECTED 2026-07-30. Saga progression IS asynchronous here (event-driven via a tracking processor), unlike quarkus-hibernate which runs the whole saga on the request thread - the two Axon stacks are NOT the same architecture, so the previously shared claim_scope_note was wrong for at least one of them. Two disclosed deviations: (a) the request thread now awaits saga completion so the response carries the terminal outcome per section 3, which is not idiomatic Axon; (d) the saga processor is given 16 segments/threads.",
      "tuning_disclosure": "CONTRACT-v2 section 9(d): axon.eventhandling.processors.OrderFulfillmentSagaProcessor thread-count=16, initial-segment-count=16. Axon default is ONE segment and ONE thread, which serialized all saga progression and produced 7613 ms median saga latency at 50/s versus 22 ms (exeris) and 34 ms (quarkus). With 16 segments: 37 ms. Comparing a one-thread default against unbounded concurrency measures the default, not the framework. The default-config ceiling is reported separately as its own finding.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "spring_boot_axon_neo4j_v1",
      "workload_shape": "A0-straight-through-superseded",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape A0: nothing parks. NOT comparison-eligible. The stacks do not execute the same work — quarkus-hibernate runs the whole saga synchronously on the request thread (transaction script), spring-hibernate runs it async via Axon Server, exeris-community via the flow scheduler. All return the terminal outcome inline, but a shared observable contract is not equivalent execution. order_create_latency_ms was briefly lifted to comparison_eligible on 2026-07-30 on the grounds that the RESOLUTION MODEL was uniform; that was wrong and is reverted — resolution uniformity does not imply execution equivalence. Superseded by shape A (minimal park), where every stack must implement dispatch/park/wake and therefore does the same shape of work.",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "_superseded_by": "spring_axon_h1_park100_v3"
    },
    "exeris_community_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "Supersedes the h2c-named v1 contract for this target. Runs under the two MUST NEVER be aggregated: protocol label, terminal-outcome resolution model and arrival rate all changed.",
      "target_app": "exeris-community",
      "framework": "exeris-community",
      "saga_orchestration": "flow-native",
      "event_backend": "events-l3-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "Runs the saga in-process on the request thread and returns the terminal outcome inline. Persists flow state to the Postgres v5 tables, so a share of this stack Postgres cost is saga state - see deployment-footprint.json, which reports it per component. The request thread blocks for the saga duration because FlowScheduler exposes no synchronous execute and no completion handle (section 9(a)).",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "exeris_community_h2c_v1",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "CORRECTED 2026-08-21 after the fail-closed guard caught the omission on the first run: this arm DOES persist the authorization outcome, via OrderSagaOrchestrator.settlePayment writing PAYMENT_AUTHORIZED/PAYMENT_DECLINED through SETTLE_PARKED_PAYMENT_SQL - a compare-and-set on the parked row, not an updateStatus() call, which is why enumerating updateStatus() sites missed it. The earlier claim here that exeris writes one fewer UPDATE per saga than spring/quarkus was wrong in our own favour. Measured n_tup_upd per inserted order on 2026-08-21: restate 4.00, spring-axon 5.00 (both arms), quarkus-lra 5.93, exeris-community 6.00 - this arm writes the MOST, and only restate writes four.",
        "verified_on": "2026-08-21",
        "verified_from": "targets/exeris-community-app/src/main/java/.../saga/OrderSagaOrchestrator.java updateStatus() call sites",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_AUTHORIZED",
          "PAYMENT_DECLINED",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30
    },
    "quarkus_lra_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "Supersedes the h2c-named v1 contract for this target. Runs under the two MUST NEVER be aggregated: protocol label, terminal-outcome resolution model and arrival rate all changed.",
      "target_app": "quarkus-hibernate",
      "framework": "quarkus",
      "saga_framework": "axon-framework",
      "event_backend": "axon-event-sourcing-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "CORRECTED 2026-07-30. The previous note (shared verbatim with the spring contract) claimed Axon performs no synchronous DB writes in the HTTP request path. That is FALSE for this stack: AxonOrderSagaCommandHandler.handle runs insert-order, reserve-inventory, charge-payment and confirm-order (or the LIFO compensation pair) synchronously on the request thread and returns the terminal outcome in the POST body. Every domain write happens in the request path here.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "quarkus_axon_neo4j_v1",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "saga_orchestration": "microprofile-lra-single-participant",
      "_engine_note": "quarkus-narayana-lra, official io.quarkus extension, support level PREVIEW. Saga enrolled as ONE participant with the LIFO unwind in application code, because the LRA spec guarantees no ordering across participants. A durable saga ENVELOPE, never 'full LRA' (§9(a)). Runs Quarkus 3.38.2, not 3.34.3.",
      "cooldown_seconds": 30
    },
    "spring_axon_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "Supersedes the h2c-named v1 contract for this target. Runs under the two MUST NEVER be aggregated: protocol label, terminal-outcome resolution model and arrival rate all changed.",
      "target_app": "spring-hibernate",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "event_backend": "axon-event-sourcing-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "CORRECTED 2026-07-30. Saga progression IS asynchronous here (event-driven via a tracking processor), unlike quarkus-hibernate which runs the whole saga on the request thread - the two Axon stacks are NOT the same architecture, so the previously shared claim_scope_note was wrong for at least one of them. Two disclosed deviations: (a) the request thread now awaits saga completion so the response carries the terminal outcome per section 3, which is not idiomatic Axon; (d) the saga processor is given 16 segments/threads.",
      "tuning_disclosure": "CONTRACT-v2 section 9(d): axon.eventhandling.processors.OrderFulfillmentSagaProcessor thread-count=16, initial-segment-count=16. Axon default is ONE segment and ONE thread, which serialized all saga progression and produced 7613 ms median saga latency at 50/s versus 22 ms (exeris) and 34 ms (quarkus). With 16 segments: 37 ms. Comparing a one-thread default against unbounded concurrency measures the default, not the framework. The default-config ceiling is reported separately as its own finding.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "spring_boot_axon_neo4j_v1",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30
    },
    "spring_axon_embedded_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "New arm (2026-08-19), supersedes nothing. Runs under this id must never be aggregated with spring_axon_h1_park100_v3: the s1 deployment unit differs.",
      "target_app": "spring-axon-embedded",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "event_backend": "axon-jdbc-event-store",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "Saga progression is asynchronous here exactly as in spring_axon_h1_park100_v3 - the same tracking processor, the same 16 threads/segments s9(d) tuning - but the events, tokens and saga state are written to Postgres over Axon's own JDBC storage engines (JdbcEventStorageEngine / JdbcTokenStore / JdbcSagaStore) rather than to Axon Server. JDBC, not JPA: every arm in this scenario is measured on JDBC because Exeris is, and an ORM on one side of the comparison would be a second variable. The s9(d) disclosure therefore applies unchanged, and one thing it does NOT cover applies only here: the token store takes a row lock per segment per claim, so Postgres carries saga-progression write traffic that the Axon Server shape sends elsewhere. That is the cost this arm is meant to make visible, not a defect.",
      "tuning_disclosure": "CONTRACT-v2 section 9(d): axon.eventhandling.processors.OrderFulfillmentSagaProcessor thread-count=16, initial-segment-count=16. Axon default is ONE segment and ONE thread, which serialized all saga progression and produced 7613 ms median saga latency at 50/s versus 22 ms (exeris) and 34 ms (quarkus). With 16 segments: 37 ms. Comparing a one-thread default against unbounded concurrency measures the default, not the framework. The default-config ceiling is reported separately as its own finding.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "spring_boot_axon_neo4j_v1",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30,
      "deployment_unit": "target-jvm + postgres",
      "deployment_unit_note": "TWO processes, against spring_axon_h1_park100_v3's three. Same jar, same saga code, same Axon Framework version; EXERIS_AXON_SERVER_ENABLED=false makes Spring Boot's Axon autoconfiguration fall back to the JPA event store, JpaTokenStore and JpaSagaStore in the SHARED Postgres, with SimpleCommandBus in-process. Registered under CONTRACT-v2 s9(e). It exists because the Axon Server shape hosts part of the saga engine in a container the target-JVM sampler cannot see, so a s8 footprint comparison against in-process saga engines partly measures WHERE the work is hosted rather than what it costs. Both shapes are reported; neither is 'the' Axon number, and the two must never be collapsed into one row."
    },
    "restate_saga_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "target_app": "restate",
      "framework": "restate-jvm-sdk",
      "framework_version": "2.9.3",
      "saga_framework": "restate-durable-execution",
      "graph_backend": "none",
      "event_backend": "restate-journal-plus-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "baseline_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "comparison_class": "durable-execution-external-server",
      "comparison_disqualifiers": [
        "protocol_mode h1 (JDK com.sun.net.httpserver facade, HTTP/1.1 only) vs h2c on the canonical saga contracts — strict-gate disqualifier, same class as spring_runtime_on_exeris_flow_neo4j_h1_v1",
        "CONTRACT-v2 s3 resolution model is INLINE (terminal outcome in the POST /api/v1/orders 200 body, k6 skips the poll loop) whereas exeris-community, spring-hibernate and spring-on-exeris are POLLED with up-to-1s quantization — cross-model saga-duration rows are forbidden without naming each stack resolution model",
        "deployment unit includes an external restate-server container outside the per-process resource sampler; whole-deployment footprint must be attributed separately from logs/restate-server-docker-stats.csv",
        "status poll is an in-memory sticky-terminal projection (status_poll_comparison_excluded), same class as the Axon stacks"
      ],
      "durability_tier_note": "restate-server 1.7 default = replicated loglet + RocksDB WAL fsync per commit batch (T2 fsync node-durable). CONTRACT-v2 s8 forbids cross-tier comparison; declare the tier per run.",
      "claim_scope_note": "Baseline-only per CONTRACT-v2-IMPLEMENTATION.md (restate row) and claim guardrail 4: descriptive single-stack Restate runs are permitted with the durability tier declared; Restate comparisons in tables or prose are NOT, until the disqualifiers above are resolved and the stack is wired as a fixed_contracts entry plus a comparative-pair-manifest row.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "descriptive_single_stack_only",
        "recommend_latency_ms": "descriptive_single_stack_only",
        "cart_add_latency_ms": "descriptive_single_stack_only",
        "cart_get_latency_ms": "descriptive_single_stack_only",
        "order_create_latency_ms": "descriptive_single_stack_only",
        "order_poll_latency_ms": "descriptive_single_stack_only"
      },
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "Same four-update path as exeris-community; see that arm's note.",
        "verified_on": "2026-08-21",
        "verified_from": "targets/restate-benchmark-app/src/main/java/.../saga/OrderSagaSqlSteps.java updateOrderStatus() call sites",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "workload_shape": "B-short-park",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "payment_callback_delay_ms": 100,
      "cooldown_seconds": 30,
      "_promotion_note": "PROMOTED from baseline_only_contracts on 2026-08-18: restate is measured alongside spring and quarkus, not kept as a descriptive aside. It qualifies on the same terms as the others — it parks on charge-payment (awakeable resolved by the gateway at the Restate ingress) and it is durable (invocation journal, RocksDB WAL), so §8's cross-tier prohibition does not exclude it.",
      "_deviation_9a": "The gateway callback resolves the awakeable at the Restate INGRESS and never enters the target JVM — platform-natural, since Restate owns durable execution, but it means this arm's per-callback cost lands in restate-server rather than in the JVM. The whole-deployment footprint still captures it; a target-JVM-only comparison would not.",
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable."
    },
    "quarkus_lra_jdbc_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "JDBC roster (2026-08-19): every arm in this scenario is measured on JDBC because Exeris is. The superseded contract ran a jar whose SAGA path was already plain JDBC, but whose other session steps (register / recommend / cart) went through an ORM - an ORM on one side of the comparison and not the other. This contract runs targets/quarkus-benchmark-app-tuned (Agroal + plain JDBC) instead of targets/quarkus-benchmark-app (Hibernate ORM + Panache). The saga surface is byte-identical - it was copied verbatim after verifying that no saga-path file in either target references Panache, an EntityManager or an @Entity. Deployment unit is unchanged at THREE processes (app JVM + LRA coordinator + Postgres). Runs under this id and under quarkus_lra_h1_park100_v3 MUST NEVER be aggregated: the artifact differs.",
      "target_app": "quarkus-lra-jdbc",
      "framework": "quarkus",
      "saga_framework": "axon-framework",
      "event_backend": "axon-event-sourcing-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "CORRECTED 2026-07-30. The previous note (shared verbatim with the spring contract) claimed Axon performs no synchronous DB writes in the HTTP request path. That is FALSE for this stack: AxonOrderSagaCommandHandler.handle runs insert-order, reserve-inventory, charge-payment and confirm-order (or the LIFO compensation pair) synchronously on the request thread and returns the terminal outcome in the POST body. Every domain write happens in the request path here.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "quarkus_lra_h1_park100_v3",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "Writes FAILED, not FAILED_UNRECOVERED (markOrderFailed); the crash runner's hardcoded guess had the latter. PAYMENT_DECLINED is NON-terminal by this arm's own definition: OrderSagaLraParticipant.status() maps CANCELLED/PAYMENT_REFUNDED to Compensated and lets PAYMENT_DECLINED fall through to Active. MEASURED 2026-08-21 on a clean no-crash run: 311 declines, 310 client-observed COMPENSATED, gate PASS, and 0 CANCELLED / 0 PAYMENT_REFUNDED / 0 ORDER_COMPENSATED in the store — this arm reports a compensation it does not perform, so declined orders stay here permanently.",
        "verified_on": "2026-08-21",
        "verified_from": "targets/quarkus-benchmark-app-tuned/src/main/java/.../axon/OrderSagaStepService.java setString(1,...) sites",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED",
          "FAILED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_AUTHORIZED",
          "PAYMENT_DECLINED",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "saga_orchestration": "microprofile-lra-single-participant",
      "_engine_note": "quarkus-narayana-lra, official io.quarkus extension, support level PREVIEW. Saga enrolled as ONE participant with the LIFO unwind in application code, because the LRA spec guarantees no ordering across participants. A durable saga ENVELOPE, never 'full LRA' (§9(a)). Runs Quarkus 3.38.2, not 3.34.3.",
      "cooldown_seconds": 30
    },
    "spring_axon_jdbc_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "JDBC roster (2026-08-19): every arm in this scenario is measured on JDBC because Exeris is. The superseded contract ran a jar whose SAGA path was already plain JDBC, but whose other session steps (register / recommend / cart) went through an ORM - an ORM on one side of the comparison and not the other. This contract runs targets/spring-benchmark-app-jdbc (spring-boot-starter-jdbc) instead of targets/spring-benchmark-app (spring-boot-starter-data-jpa). The saga surface is byte-identical - it was copied verbatim after verifying that no saga-path file in either target references an EntityManager, an @Entity or a JpaRepository. Deployment unit is unchanged at THREE processes (target JVM + Postgres + Axon Server). SECOND, INDEPENDENT reason not to aggregate with the superseded id: tracking tokens and saga state are now DURABLE in Postgres on this arm. Axon Server is an event store with no token-store or saga-store API, and Axon's starter had been falling back to in-memory, so the superseded runs did not pay for a durable saga engine at all.",
      "target_app": "spring-axon-jdbc",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "event_backend": "axon-event-sourcing-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "CORRECTED 2026-07-30. Saga progression IS asynchronous here (event-driven via a tracking processor), unlike quarkus-hibernate which runs the whole saga on the request thread - the two Axon stacks are NOT the same architecture, so the previously shared claim_scope_note was wrong for at least one of them. Two disclosed deviations: (a) the request thread now awaits saga completion so the response carries the terminal outcome per section 3, which is not idiomatic Axon; (d) the saga processor is given 16 segments/threads.",
      "tuning_disclosure": "CONTRACT-v2 section 9(d): axon.eventhandling.processors.OrderFulfillmentSagaProcessor thread-count=16, initial-segment-count=16. Axon default is ONE segment and ONE thread, which serialized all saga progression and produced 7613 ms median saga latency at 50/s versus 22 ms (exeris) and 34 ms (quarkus). With 16 segments: 37 ms. Comparing a one-thread default against unbounded concurrency measures the default, not the framework. The default-config ceiling is reported separately as its own finding.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "spring_axon_h1_park100_v3",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "Persists the authorization outcome as a row state (PaymentCallbackController:74), so five UPDATEs per saga against exeris/restate's four.",
        "verified_on": "2026-08-21",
        "verified_from": "targets/spring-benchmark-app-jdbc/src/main/java/.../axon/{InventoryService,PaymentService,PaymentCallbackController,OrderConfirmationService}.java",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_AUTHORIZED",
          "PAYMENT_DECLINED",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30
    },
    "spring_axon_embedded_jdbc_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "JDBC roster (2026-08-19): every arm in this scenario is measured on JDBC because Exeris is. The superseded contract ran a jar whose SAGA path was already plain JDBC, but whose other session steps (register / recommend / cart) went through an ORM - an ORM on one side of the comparison and not the other. This contract runs targets/spring-benchmark-app-jdbc instead of targets/spring-benchmark-app. Deployment unit is unchanged at TWO processes (target JVM + Postgres). Against spring_axon_jdbc_h1_park100_v3 the arms now differ on exactly ONE axis - where EVENTS live - because tokens and saga state are durable in Postgres on both.",
      "target_app": "spring-axon-embedded-jdbc",
      "framework": "spring-boot",
      "saga_framework": "axon-framework",
      "event_backend": "axon-jdbc-event-store",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "Saga progression is asynchronous here exactly as in spring_axon_h1_park100_v3 - the same tracking processor, the same 16 threads/segments s9(d) tuning - but the events, tokens and saga state are written to Postgres over Axon's own JDBC storage engines (JdbcEventStorageEngine / JdbcTokenStore / JdbcSagaStore) rather than to Axon Server. JDBC, not JPA: every arm in this scenario is measured on JDBC because Exeris is, and an ORM on one side of the comparison would be a second variable. The s9(d) disclosure therefore applies unchanged, and one thing it does NOT cover applies only here: the token store takes a row lock per segment per claim, so Postgres carries saga-progression write traffic that the Axon Server shape sends elsewhere. That is the cost this arm is meant to make visible, not a defect.",
      "tuning_disclosure": "CONTRACT-v2 section 9(d): axon.eventhandling.processors.OrderFulfillmentSagaProcessor thread-count=16, initial-segment-count=16. Axon default is ONE segment and ONE thread, which serialized all saga progression and produced 7613 ms median saga latency at 50/s versus 22 ms (exeris) and 34 ms (quarkus). With 16 segments: 37 ms. Comparing a one-thread default against unbounded concurrency measures the default, not the framework. The default-config ceiling is reported separately as its own finding.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "spring_axon_embedded_h1_park100_v3",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "Same application as the spring_axon_jdbc arm; the difference is the Axon deployment unit (§1), not the domain writes.",
        "verified_on": "2026-08-21",
        "verified_from": "same application jar as spring_axon_jdbc_h1_park100_v3 (targets/spring-benchmark-app-jdbc)",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_AUTHORIZED",
          "PAYMENT_DECLINED",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30,
      "deployment_unit": "target-jvm + postgres",
      "deployment_unit_note": "TWO processes, against spring_axon_h1_park100_v3's three. Same jar, same saga code, same Axon Framework version; EXERIS_AXON_SERVER_ENABLED=false makes Spring Boot's Axon autoconfiguration fall back to the JPA event store, JpaTokenStore and JpaSagaStore in the SHARED Postgres, with SimpleCommandBus in-process. Registered under CONTRACT-v2 s9(e). It exists because the Axon Server shape hosts part of the saga engine in a container the target-JVM sampler cannot see, so a s8 footprint comparison against in-process saga engines partly measures WHERE the work is hosted rather than what it costs. Both shapes are reported; neither is 'the' Axon number, and the two must never be collapsed into one row."
    },
    "spring_on_exeris_h1_park100_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park100-v3",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "comparison_eligible_pending_gate",
      "supersedes_note": "Added 2026-08-20 under CONTRACT-v2.2. s1 has listed spring-on-exeris as a measured arm since v2.1 while no park100 contract id existed for it, so it could not be run and was silently absent from every shape-B table. The only prior contract id for this arm is spring_runtime_on_exeris_flow_neo4j_h1_v1, which is graph-era (graph_backend neo4j-bolt) and superseded when v2.1 removed the graph from this scenario entirely. Numbers from that id are NOT continuous with this one.",
      "target_app": "spring-on-exeris",
      "framework": "spring-boot",
      "saga_framework": "exeris-spring-runtime-flow",
      "event_backend": "events-l3-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "CONTRACT-v2.2 s1: spring-on-exeris is a 2-process deployment unit (app JVM with the kernel in-process, plus Postgres), the same unit as exeris-community. Hosting is COMPATIBILITY mode: Spring MVC @RestController dispatch is overlaid on the Exeris ingress via ExerisCompatAutoConfiguration (ADR-011). Saga orchestration is exeris-spring-runtime-flow - algorithmically the same Flow engine exeris-community runs, NOT Axon Framework - so this arm and exeris-community share a saga engine and differ in the web/programming layer above it. That is the comparison this arm exists to make, and it is a Pure-vs-Compatibility axis (CLAUDE.md): results MUST be labelled compat and MUST NOT be collapsed with exeris-community's pure-mode row.",
      "tuning_disclosure": "None applied beyond the roster-wide equalization every arm receives: same JDK, same -XX:MaxRAM/-Xmx, same pgjdbc query-protocol parameters, same DB pool size, same CPU pin set. s9(e): the PURE configuration (exeris-spring-runtime-app-pure) is available and cheaper and is deliberately NOT the measured one here - this arm is measured in its more expensive configuration.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "spring_runtime_on_exeris_flow_neo4j_h1_v1",
      "workload_shape": "B-short-park",
      "workload_shape_note": "CONTRACT-v2 §2.1 shape B — short park (~100 ms). Answers: what does one asynchronous hop cost, and does the engine handle it without pinning a resource per in-flight saga? Meaningful: CPU per saga, resources held while parked (threads, DB connections, sockets), wake throughput. Read with care: end-to-end latency is dominated by the gateway delay, identical on every stack, so it loses most of its discriminating power — report it only alongside the delay. LIMITATION measured 2026-08-20: at the §2 normative rate the in-flight saga population is 4.7–16.7, at which 'resources held per in-flight saga' does NOT discriminate thread-per-saga from state-per-saga — observed thread counts are dominated by fixed framework pools (66–115 threads regardless of 4.7 vs 16.7 in flight) and the ratio inverts, ranking the arm with the fewest in-flight sagas worst. Shape C is where that metric becomes measurable.",
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 100,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30,
      "framework_version": "3.5.14",
      "host_runtime": "exeris-spring-runtime",
      "host_runtime_version": "0.7.0",
      "host_runtime_mode": "compatibility",
      "saga_orchestration": "flow-native"
    },
    "exeris_community_h1_park250_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park250-v3-recon",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "non_eligible",
      "supersedes_note": "Derived from exeris_community_h1_park100_v3 on 2026-08-21 by changing ONLY the park depth to 250 ms.",
      "target_app": "exeris-community",
      "framework": "exeris-community",
      "saga_orchestration": "flow-native",
      "event_backend": "events-l3-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "Correctness/capacity recon only. Not comparison-eligible and not a latency or throughput source under any circumstances: the park depth is a harness constant that dominates every duration measured under it.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "exeris_community_h2c_v1",
      "workload_shape": "C-approach-recon-park250",
      "workload_shape_note": "RECON, not a publishable shape. §2.1 defines shape B at ~100 ms and shape C as a long park released by the harness; these three sit between them and exist for one purpose: shape B's in-flight population is rate x saga duration, which at 38/s and a 100 ms park is 5-14 sagas — measured 2026-08-21 to be too small to rank restart survival (the same arm, same config, gave 0/5 and 5/7 on consecutive runs). The ladder finds the park depth at which the cohort is large enough to measure, and whether the stack starts holding resources per parked saga before it gets there. §2.1 phase C of shape C is where this metric is declared to live, at N = 10 000+; these runs are the approach to it. NEVER aggregate with shape B or with each other, and never quote latency from them.",
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "CORRECTED 2026-08-21 after the fail-closed guard caught the omission on the first run: this arm DOES persist the authorization outcome, via OrderSagaOrchestrator.settlePayment writing PAYMENT_AUTHORIZED/PAYMENT_DECLINED through SETTLE_PARKED_PAYMENT_SQL - a compare-and-set on the parked row, not an updateStatus() call, which is why enumerating updateStatus() sites missed it. The earlier claim here that exeris writes one fewer UPDATE per saga than spring/quarkus was wrong in our own favour. Measured n_tup_upd per inserted order on 2026-08-21: restate 4.00, spring-axon 5.00 (both arms), quarkus-lra 5.93, exeris-community 6.00 - this arm writes the MOST, and only restate writes four.",
        "verified_on": "2026-08-21",
        "verified_from": "targets/exeris-community-app/src/main/java/.../saga/OrderSagaOrchestrator.java updateStatus() call sites",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_AUTHORIZED",
          "PAYMENT_DECLINED",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 250,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30
    },
    "exeris_community_h1_park500_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park500-v3-recon",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "non_eligible",
      "supersedes_note": "Derived from exeris_community_h1_park100_v3 on 2026-08-21 by changing ONLY the park depth to 500 ms.",
      "target_app": "exeris-community",
      "framework": "exeris-community",
      "saga_orchestration": "flow-native",
      "event_backend": "events-l3-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "Correctness/capacity recon only. Not comparison-eligible and not a latency or throughput source under any circumstances: the park depth is a harness constant that dominates every duration measured under it.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "exeris_community_h2c_v1",
      "workload_shape": "C-approach-recon-park500",
      "workload_shape_note": "RECON, not a publishable shape. §2.1 defines shape B at ~100 ms and shape C as a long park released by the harness; these three sit between them and exist for one purpose: shape B's in-flight population is rate x saga duration, which at 38/s and a 100 ms park is 5-14 sagas — measured 2026-08-21 to be too small to rank restart survival (the same arm, same config, gave 0/5 and 5/7 on consecutive runs). The ladder finds the park depth at which the cohort is large enough to measure, and whether the stack starts holding resources per parked saga before it gets there. §2.1 phase C of shape C is where this metric is declared to live, at N = 10 000+; these runs are the approach to it. NEVER aggregate with shape B or with each other, and never quote latency from them.",
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "CORRECTED 2026-08-21 after the fail-closed guard caught the omission on the first run: this arm DOES persist the authorization outcome, via OrderSagaOrchestrator.settlePayment writing PAYMENT_AUTHORIZED/PAYMENT_DECLINED through SETTLE_PARKED_PAYMENT_SQL - a compare-and-set on the parked row, not an updateStatus() call, which is why enumerating updateStatus() sites missed it. The earlier claim here that exeris writes one fewer UPDATE per saga than spring/quarkus was wrong in our own favour. Measured n_tup_upd per inserted order on 2026-08-21: restate 4.00, spring-axon 5.00 (both arms), quarkus-lra 5.93, exeris-community 6.00 - this arm writes the MOST, and only restate writes four.",
        "verified_on": "2026-08-21",
        "verified_from": "targets/exeris-community-app/src/main/java/.../saga/OrderSagaOrchestrator.java updateStatus() call sites",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_AUTHORIZED",
          "PAYMENT_DECLINED",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 500,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30
    },
    "exeris_community_h1_park1000_v3": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "protocol_note": "h1 because k6 CANNOT drive cleartext HTTP/2 (verified: curl --http2-prior-knowledge and h2load both negotiate h2c against the same endpoint where k6 gets HTTP/1.1; grafana/k6#970 open since 2019). All three targets DO serve h2c, so this is a driver limit, not a target limit. h1-for-all is fair because it is identical on every stack; an h2c track needs a different driver and its own profile key.",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "resolution_model": "inline",
      "resolution_model_note": "CONTRACT-v2 section 3 request-response: the POST /api/v1/orders body carries the terminal outcome, so k6 never polls. Uniform across all three contracts as of 2026-07-30 - previously exeris-community and spring-hibernate were polled with 1 s quantization, which made their measured saga duration a flat ~1010 ms of client sleep.",
      "load_model": "constant-arrival-rate",
      "measurement_arrival_rate_per_s": 50,
      "arrival_rate_note": "50 sessions/s chosen from a measured sweep (3/25/50/100/200). 50 is the highest rate clean on ALL stacks: at 100 exeris-community loses its entire measurement phase to connection resets. VU counts are a POOL CEILING under this executor, not concurrency - the previous virtual_users:100 field described a closed-loop model this scenario does not run.",
      "measured_peak_concurrency": 342,
      "graph_backend": "none",
      "ramp_up_seconds": 60,
      "warmup_seconds": 300,
      "measurement_window_seconds": 900,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "workload_profile_key": "e2e-shop-order-saga-community-h1-loopback-runtime-k6-r38-park1000-v3-recon",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "descriptive_only",
      "claim_scope": "non_eligible",
      "supersedes_note": "Derived from exeris_community_h1_park100_v3 on 2026-08-21 by changing ONLY the park depth to 1000 ms.",
      "target_app": "exeris-community",
      "framework": "exeris-community",
      "saga_orchestration": "flow-native",
      "event_backend": "events-l3-outbox",
      "comparison_class": "platform-natural-stack",
      "claim_scope_note": "Correctness/capacity recon only. Not comparison-eligible and not a latency or throughput source under any circumstances: the park depth is a harness constant that dominates every duration measured under it.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "comparison_eligible",
        "cart_add_latency_ms": "comparison_eligible",
        "cart_get_latency_ms": "comparison_eligible",
        "recommend_latency_ms": "comparison_eligible_platform_natural_only",
        "order_create_latency_ms": "descriptive_per_stack_only_execution_not_equivalent",
        "order_poll_latency_ms": "not_applicable_inline_resolution"
      },
      "per_step_metric_claim_scope_note": "recommend_latency_ms is platform-natural ONLY: the stacks do NOT issue the same work. quarkus/spring send ONE Cypher query expressing the two-hop join server-side; exeris-community issues 1+N traversals because GraphTraversal (exeris-kernel-spi) carries exactly one GraphEdgeDescriptor and no API accepts a heterogeneous edge path. Measured throughput-matched: 2.6x the Neo4j CPU and 2.5x the latency for exeris. Do not read it as runtime speed. order_create_latency_ms IS eligible now - it was earned by making every stack section-3 compliant, not by relabelling.",
      "superseded_contract": "exeris_community_h2c_v1",
      "workload_shape": "C-approach-recon-park1000",
      "workload_shape_note": "RECON, not a publishable shape. §2.1 defines shape B at ~100 ms and shape C as a long park released by the harness; these three sit between them and exist for one purpose: shape B's in-flight population is rate x saga duration, which at 38/s and a 100 ms park is 5-14 sagas — measured 2026-08-21 to be too small to rank restart survival (the same arm, same config, gave 0/5 and 5/7 on consecutive runs). The ladder finds the park depth at which the cohort is large enough to measure, and whether the stack starts holding resources per parked saga before it gets there. §2.1 phase C of shape C is where this metric is declared to live, at N = 10 000+; these runs are the approach to it. NEVER aggregate with shape B or with each other, and never quote latency from them.",
      "domain_row_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 discipline applied to the DOMAIN ROW. This is a DIFFERENT vocabulary from terminal_vocabulary above: that one describes the HTTP response field, where COMPENSATED is a terminal token that never appears in the orders table, while CANCELLED appears in the table and is unknown to it. Reading one for the other is a category error, and the W3a crash runner made it by hardcoding a guess. Verified 2026-08-21 against the status write sites named in verified_from. A consumer MUST fail closed on any status token observed in the table that is in neither list, rather than bucketing it as terminal.",
        "_note": "CORRECTED 2026-08-21 after the fail-closed guard caught the omission on the first run: this arm DOES persist the authorization outcome, via OrderSagaOrchestrator.settlePayment writing PAYMENT_AUTHORIZED/PAYMENT_DECLINED through SETTLE_PARKED_PAYMENT_SQL - a compare-and-set on the parked row, not an updateStatus() call, which is why enumerating updateStatus() sites missed it. The earlier claim here that exeris writes one fewer UPDATE per saga than spring/quarkus was wrong in our own favour. Measured n_tup_upd per inserted order on 2026-08-21: restate 4.00, spring-axon 5.00 (both arms), quarkus-lra 5.93, exeris-community 6.00 - this arm writes the MOST, and only restate writes four.",
        "verified_on": "2026-08-21",
        "verified_from": "targets/exeris-community-app/src/main/java/.../saga/OrderSagaOrchestrator.java updateStatus() call sites",
        "table": "orders",
        "column": "status",
        "on_undeclared_token": "fail_closed",
        "compensated_tokens": [
          "CANCELLED"
        ],
        "_compensated_tokens_note": "The subset of terminal_tokens that means the backward-recovery path RAN and left the domain row in a compensated state. Verified 2026-08-21: every arm writes CANCELLED as the last compensation step. Used by the §4.1 gate as a corroboration leg, because the gate's observed_compensations counts a CLIENT-VISIBLE token, and on 2026-08-21 quarkus-lra passed 310/310 on that token with zero compensated rows in the store.",
        "terminal_tokens": [
          "COMPLETED",
          "CANCELLED"
        ],
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "PAYMENT_AUTHORIZED",
          "PAYMENT_DECLINED",
          "PAYMENT_REFUNDED",
          "CONFIRMED"
        ]
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "payment_callback_delay_ms": 1000,
      "_shape_note": "CONTRACT-v2 §2.1 shape B (short park): S_pay dispatches to the external payment gateway and parks; the gateway answers after ~100 ms. Every stack implements dispatch -> park -> external event -> wake, so they execute the same SHAPE of work. Graph removed from the scenario (§2). Results under this key MUST NOT be aggregated with shape A0 (…-inline-r50-v2, straight-through, retired) nor with shape A (…-park1-v3, ~1 ms park). CORRECTED 2026-08-20 (v2.2): these ids were renamed park1 -> park100 on 2026-08-19 because the roster had always run a 100 ms park under shape-A ids - the label was wrong, not the workload. That rename moved the ids and the note but left workload_shape=A-minimal-park and payment_callback_delay_ms=1 in every entry, so the machine-readable shape still said A while the id and the run said B. §8 requires every reported figure to name its shape, and a consumer reading these fields would have named it wrong. Shape-B metric rules apply: end-to-end latency is SECONDARY and must be reported alongside the 100 ms delay; the headline metrics are CPU per saga and resources held per in-flight saga.",
      "cooldown_seconds": 30
    }
  },
  "baseline_only_contracts": {
    "_note": "Contracts that are RUNNABLE but deliberately NOT comparison-eligible. Kept OUT of fixed_contracts and OUT of graph_tracks.*.required_contracts on purpose, so no comparative tooling that walks those two structures can ever pick them up. The saga campaign runner consults this namespace only to derive a --contract-id for a descriptive baseline run, and stamps baseline_only=true on the run. Promotion out of here requires a fixed_contracts entry AND a comparative-pair-manifest.json row AND resolution of the listed disqualifiers.",
    "restate_saga_h1_v1": {
      "scenario_id": "e2e-shop-order-saga",
      "tier": "community",
      "protocol_mode": "h1",
      "transport_mode": "loopback-h1",
      "benchmark_family": "runtime-k6-e2e",
      "jvm_class": "jvm",
      "target_app": "restate",
      "framework": "restate-jvm-sdk",
      "framework_version": "2.9.3",
      "saga_framework": "restate-durable-execution",
      "graph_backend": "neo4j-bolt",
      "event_backend": "restate-journal-plus-outbox",
      "virtual_users": 100,
      "ramp_up_seconds": 60,
      "warmup_seconds": 60,
      "measurement_window_seconds": 180,
      "think_time_ms_min": 800,
      "think_time_ms_max": 2500,
      "payment_fail_rate": 0.03,
      "payload_profile": "orders-aggregate-v1",
      "required_profile": "perf-box-amd64",
      "seed_manifest_refs": {
        "manifest_ref": "scenarios/e2e-shop-order-saga/seed/seed-manifest.json",
        "manifest_version": "1",
        "verification_script": "scenarios/e2e-shop-order-saga/seed/verify-seed.sh"
      },
      "fairness_gates": {
        "min_completed_orders": 900,
        "error_rate_max": 0.02,
        "saga_success_rate_min": 0.98,
        "compensation_invocation_rate_max": 0.05,
        "ci_level": 0.95,
        "relative_error_pct_max": 15
      },
      "maturity": "baseline_only",
      "claim_scope": "descriptive_single_stack_only",
      "comparison_class": "durable-execution-external-server",
      "comparison_disqualifiers": [
        "protocol_mode h1 (JDK com.sun.net.httpserver facade, HTTP/1.1 only) vs h2c on the canonical saga contracts — strict-gate disqualifier, same class as spring_runtime_on_exeris_flow_neo4j_h1_v1",
        "CONTRACT-v2 s3 resolution model is INLINE (terminal outcome in the POST /api/v1/orders 200 body, k6 skips the poll loop) whereas exeris-community, spring-hibernate and spring-on-exeris are POLLED with up-to-1s quantization — cross-model saga-duration rows are forbidden without naming each stack resolution model",
        "deployment unit includes an external restate-server container outside the per-process resource sampler; whole-deployment footprint must be attributed separately from logs/restate-server-docker-stats.csv",
        "status poll is an in-memory sticky-terminal projection (status_poll_comparison_excluded), same class as the Axon stacks"
      ],
      "durability_tier_note": "restate-server 1.7 default = replicated loglet + RocksDB WAL fsync per commit batch (T2 fsync node-durable). CONTRACT-v2 s8 forbids cross-tier comparison; declare the tier per run.",
      "claim_scope_note": "Baseline-only per CONTRACT-v2-IMPLEMENTATION.md (restate row) and claim guardrail 4: descriptive single-stack Restate runs are permitted with the durability tier declared; Restate comparisons in tables or prose are NOT, until the disqualifiers above are resolved and the stack is wired as a fixed_contracts entry plus a comparative-pair-manifest row.",
      "per_step_metric_claim_scope": {
        "auth_latency_ms": "descriptive_single_stack_only",
        "recommend_latency_ms": "descriptive_single_stack_only",
        "cart_add_latency_ms": "descriptive_single_stack_only",
        "cart_get_latency_ms": "descriptive_single_stack_only",
        "order_create_latency_ms": "descriptive_single_stack_only",
        "order_poll_latency_ms": "descriptive_single_stack_only"
      },
      "terminal_vocabulary": {
        "_comment": "CONTRACT-v2 §3.1 (normative): declared, never inferred. The harness MUST read this and MUST NOT guess either the field or the tokens. Verified against target sources on 2026-07-31; a stack whose declaration is contradicted at preflight does not run. Adding tolerance for a second field name instead of a declaration is what let a blind detector report a clean zero for a whole campaign.",
        "resolution_model": "inline_with_polled_fallback",
        "terminal_field": "status",
        "poll_terminal_field": "status",
        "terminal_tokens": {
          "COMPLETED": "COMPLETED",
          "COMPENSATED": "COMPENSATED",
          "FAILED_UNRECOVERED": "FAILED_UNRECOVERED"
        },
        "non_terminal_tokens": [
          "SAGA_INITIATED",
          "INVENTORY_RESERVED",
          "PAYMENT_PROCESSING",
          "COMPENSATING",
          "COMPLETING",
          "ACCEPTED",
          "PARKED"
        ]
      },
      "_superseded_by": "restate_saga_h1_park100_v3"
    }
  }
}
