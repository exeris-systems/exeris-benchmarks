{
  "scenario_id": "routing-404",
  "version": "1",
  "benchmark_family": "runtime",
  "target_classification": "runtime-routing-baseline",
  "description": "Fast-path negative routing cost. GET /does-not-exist must produce 404 without full route trie traversal.",
  "endpoint": "GET /does-not-exist",
  "mode": "routing-negative",
  "tier": "community",
  "driver": ["wrk", "k6"],
  "transport": ["h1"],
  "loopback": true,
  "loopback_caveat": "Results are not equivalent to network-path benchmarks. transport_mode=loopback-h1 is mandatory in all result artifacts.",
  "concurrency": {
    "connections": 100,
    "threads": 4
  },
  "fixed_contracts": {
    "community_h1_v1": {
      "tier": "community",
      "protocol_mode": "h1",
      "benchmark_family": "runtime-wrk",
      "transport_mode": "loopback-h1",
      "threads": 4,
      "connections": 100,
      "warmup_seconds": 60,
      "duration_seconds": 120,
      "required_profile": "perf-box-amd64"
    }
  },
  "claim_scope": "exploratory"
}
