# Enterprise runtime targets

Protocol-scoped target profiles for Enterprise tier.

- `h1/` — HTTP/1.1 fallback path
- `h2/` — HTTP/2 fallback path
- `h3/` — HTTP/3 (QUIC)

Execution can be public (docker/jar) or private (`START_MODE=external`).
