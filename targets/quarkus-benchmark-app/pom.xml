<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>eu.exeris.benchmarks</groupId>
  <artifactId>quarkus-benchmark-app</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>Exeris :: Benchmarks :: Quarkus Benchmark App</name>
  <description>Standalone Quarkus benchmark target app.</description>

  <properties>
    <java.version>26</java.version>
    <maven.compiler.release>${java.version}</maven.compiler.release>
    <maven.compiler.plugin.version>3.15.0</maven.compiler.plugin.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
    <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
    <quarkus.platform.version>3.34.3</quarkus.platform.version>
    <quarkus.plugin.version>${quarkus.platform.version}</quarkus.plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-agroal</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-orm-panache</artifactId>
    </dependency>
    <!-- CONTRACT-v2 §9(a) — the saga engine for this arm, decided in REMEDIATION-PLAN D1.
         First-party (io.quarkus, platform BOM, Red Hat & IBM), support level PREVIEW,
         which goes into the reproducibility metadata. The saga is enrolled as a SINGLE
         participant: the MicroProfile LRA spec guarantees NO ordering across
         participants ("makes no guarantees about when Compensate method will be
         invoked"), and §2 requires LIFO, so the unwind is done in application code. -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-narayana-lra</artifactId>
    </dependency>

    <!-- The REST client quarkus-narayana-lra uses to reach the coordinator.
         quarkus-rest-client-jackson, i.e. the REACTIVE client, deliberately: it pairs
         with quarkus-rest-jackson so the whole arm stays on Quarkus REST. The plain
         quarkus-rest-client satisfied the LRA build step but left the runtime reaching
         for a RESTEasy Classic class (ContextualExecutors) on the first request that
         started an LRA — build-passes, then throws.
         Keeping the reactive stack is not only about making it start: @Compensate and
         @Complete then run on the Vert.x event loop instead of pinning a worker thread
         per in-flight coordinator round trip, which is exactly the exhaustion mode a
         parking saga benchmark at a fixed arrival rate is built to expose. -->

    <!-- The CLASSIC client, and only the client. The SERVER stays on Quarkus REST
         (quarkus-rest-jackson) so every endpoint this benchmark measures is still on
         the reactive path — the point of the arm is unchanged.
         Needed because quarkus-narayana-lra 3.34.3 pulls lra-client 1.1.0.Final, which
         pulls org.jboss.resteasy.microprofile:microprofile-rest-client (the CLASSIC MP
         REST Client). NarayanaLRAClient.createCoordinatorClient builds its client down
         that path regardless of which reactive client is present, and then fails on
         org.jboss.resteasy.concurrent.ContextualExecutors from resteasy-core. Evidence:
         mvn dependency:tree, and the NoClassDefFoundError on the first request that
         starts an LRA. -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-resteasy-client</artifactId>
    </dependency>

    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-narayana-jta</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-virtual-threads</artifactId>
    </dependency>
    <dependency>
      <groupId>org.neo4j.driver</groupId>
      <artifactId>neo4j-java-driver</artifactId>
      <version>5.28.8</version>
    </dependency>
    <dependency>
      <groupId>org.axonframework</groupId>
      <artifactId>axon-messaging</artifactId>
      <version>4.10.3</version>
    </dependency>
    <dependency>
      <groupId>org.axonframework</groupId>
      <artifactId>axon-configuration</artifactId>
      <version>4.10.3</version>
    </dependency>
    <dependency>
      <groupId>org.axonframework</groupId>
      <artifactId>axon-server-connector</artifactId>
      <version>4.10.3</version>
    </dependency>
    <dependency>
      <groupId>com.nimbusds</groupId>
      <artifactId>nimbus-jose-jwt</artifactId>
      <version>10.9</version>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>io.quarkus</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <version>${quarkus.plugin.version}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <enablePreview>false</enablePreview>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <useModulePath>false</useModulePath>
          <systemPropertyVariables>
            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>native</id>
      <properties>
        <quarkus.package.type>native</quarkus.package.type>
        <quarkus.native.enabled>true</quarkus.native.enabled>
        <quarkus.native.container-build>false</quarkus.native.container-build>
      </properties>
    </profile>
  </profiles>
</project>
